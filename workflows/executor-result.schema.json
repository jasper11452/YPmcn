{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ypmcn.local/schemas/executor-result.schema.json",
  "title": "YPmcn Codex Executor Result",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "task_id",
    "status",
    "session_id",
    "base_sha",
    "head_sha",
    "commit",
    "changed_files",
    "tests",
    "evidence_paths",
    "context_expansions",
    "known_risks",
    "blocked_reason"
  ],
  "properties": {
    "schema_version": { "const": "2.2" },
    "task_id": { "type": "string", "minLength": 1 },
    "status": { "enum": ["PASS", "BLOCKED"] },
    "session_id": { "type": ["string", "null"] },
    "base_sha": { "$ref": "#/$defs/shaOrNull" },
    "head_sha": { "$ref": "#/$defs/shaOrNull" },
    "commit": { "$ref": "#/$defs/shaOrNull" },
    "changed_files": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "tests": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["command", "result", "evidence"],
        "properties": {
          "command": { "type": "string", "minLength": 1 },
          "result": { "enum": ["PASS", "FAIL", "NOT_RUN"] },
          "evidence": { "type": "string" }
        }
      }
    },
    "evidence_paths": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "context_expansions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["reason", "source_ref"],
        "properties": {
          "reason": { "type": "string", "minLength": 1 },
          "source_ref": { "type": "string", "minLength": 1 }
        }
      }
    },
    "known_risks": {
      "type": "array",
      "items": { "type": "string" }
    },
    "blocked_reason": { "type": ["string", "null"] }
  },
  "$defs": {
    "shaOrNull": {
      "anyOf": [
        { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        { "type": "null" }
      ]
    }
  }
}
